# Generated / cache output. `make precommit` runs `fmt-check` (prettier --check .)
# FIRST and scans the whole tree, so anything written here would otherwise fail
# the gate on a non-source file (e.g. pytest's .pytest_cache/README.md, or an
# html/lcov coverage reporter). Add new generated output dirs here.
coverage/
.pytest_cache/
nas_proxy/out/
clean_adblock/dist/

# Human-maintained persona definitions for the Jules routines (see AGENTS.md).
# Prose/markdown, not gated — don't let Prettier reformat them.
.jules/

# Mutation-testing artifacts (non-blocking scaffold: make mutate-js / mutate-py)
reports/
mutants/
.mutmut-cache/

# Research course materials
research/

